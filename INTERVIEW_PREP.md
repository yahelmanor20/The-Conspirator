<div dir="rtl">

# 💼 הכנה לראיון עבודה – פרויקט "הקונספירטור"

קובץ זה שונה ממדריך המבחן ([EXAM_PREP.md](EXAM_PREP.md)). בראיון עבודה לא בודקים אם שיננת —
בודקים **איך אתה חושב**, **למה קיבלת החלטות**, ו**איך אתה מסביר את עצמך**.
לכל שאלה כתובה הכוונה קצרה איך לענות טוב. תרגל לענות בקול, בקצב נינוח.

---

## חלק 1 – "ספר לי על הפרויקט שלך" (Pitch של 60–90 שניות)

זו כמעט תמיד השאלה הראשונה. הכן תשובה קצרה, ברורה ובטוחה.

**שאלות:**
1. ספר על הפרויקט שלך במשפט-שניים. מה הוא עושה ולמי הוא מיועד?
2. אילו טכנולוגיות בחרת ולמה דווקא אותן? (Node.js, Express, MongoDB/Mongoose, React)
3. מה היה החלק שאתה הכי גאה בו?
4. מה היה החלק הכי קשה / מה למדת ממנו?

**איך לענות (תבנית ל-Pitch):**
> "בניתי אפליקציית Full-Stack בשם 'הקונספירטור' שמאפשרת למשתמשים ליצור, לפרסם,
> לדרג ולהגיב על תיאוריות קונספירציה. ה-Backend הוא REST API ב-Node.js + Express,
> עם MongoDB דרך Mongoose, וה-Frontend ב-React. יש גם שירות שמייצר קונספירציות
> אקראיות מתוך מאגרי נתונים ותבניות משפט. בניתי את זה בארכיטקטורה מסודרת של
> שכבות — routes, controllers, services, models — כדי שיהיה קל לתחזק ולהרחיב."

**טיפ:** סיים את ה-Pitch בנקודה שמזמינה שאלת המשך שאתה שולט בה (למשל "בניתי את זה בשכבות").

---

## חלק 2 – החלטות ארכיטקטורה ("למה ככה ולא אחרת")

מראיינים אוהבים לשמוע **נימוק**, לא רק "ככה עשיתי".

5. למה פיצלת את הקוד ל-`routes`, `controllers`, `services`, `models`?
   מה היתרון על פני קובץ אחד גדול? (Separation of concerns, תחזוקה, בדיקות)
6. למה הוצאת את מחולל הקונספירציות ל-[services/conspiracyGenerator.js](services/conspiracyGenerator.js)
   ולא כתבת אותו בתוך ה-controller?
7. למה בחרת ב-MongoDB (NoSQL) ולא במסד יחסי כמו SQL? מה מתאים לפרויקט הזה?
   - רמז טוב: מבנה הנתונים גמיש, התגובות מקוננות בתוך הקונספירציה (embedded documents).
8. למה התגובות (`comments`) שמורות **בתוך** מסמך הקונספירציה ולא בקולקשן נפרד?
   מתי הגישה הזו טובה ומתי היא תהפוך לבעיה?
9. הסבר את התבנית של `getConspiracy` כ-middleware שמשותף להרבה routes
   ([conspiracyRouter.js:13-37](routes/conspiracyRouter.js#L13)). למה זה עדיף על שכפול הקוד בכל פונקציה?

---

## חלק 3 – צלילה טכנית ל-Backend

10. עבור אותי על מסלול בקשה מלא: משתמש לוחץ 👍 בדפדפן — מה קורה עד שהמסד מתעדכן?
    (fetch → route → middleware `getConspiracy` → `likeConspiracy` → `save()` → response → refetch)
11. למה כמעט כל הפונקציות ב-controller הן `async/await`? מה היה קורה בלי `await` על `save()`?
12. למה כל פעולה עטופה ב-`try/catch`? איזה status code אתה מחזיר על שגיאה ולמה?
13. הסבר את ההבדל בין קודי הסטטוס שאתה מחזיר: `200`, `201`, `400`, `404`, `500`.
    למה ביצירה החזרת `201`?
14. מה זה CORS, ולמה היה צריך `app.use(cors())` ב-[server.js:13](server.js#L13)?
    מה היה קורה בלעדיו כשה-Frontend (פורט 3000) פונה ל-Backend (פורט 5000)?
15. הסבר את `req.body.likes ?? 0`. מה ההבדל בין `??` ל-`||` ולמה זה משנה כאן?

---

## חלק 4 – צלילה טכנית ל-Frontend (React)

16. למה החזקת את ה-state של הקונספירציות ב-`App` והעברת אותו לילדים כ-props
    ולא בכל קומפוננטה בנפרד? (Lifting state up)
17. מה עושה `useEffect(() => {...}, [])` ב-[App.js:32](client/src/App.js#L32)? מתי הוא רץ?
18. למה React דורש `key` ב-`conspiracies.map(...)`? מה הסיכון אם ה-key לא ייחודי?
19. הסבר את הזרימה כש-`fetchConspiracies` מועבר כ-prop לכל קומפוננטה —
    איך הילד "מודיע" להורה שצריך לרענן את הרשימה? (callback prop)
20. אחרי לייק אתה גם מעדכן `conspiracy.likes += 1` וגם קורא ל-`fetchConspiracies()`.
    למה שניהם? מה היתרון/חיסרון של עדכון אופטימי מול refetch מלא?

---

## חלק 5 – ביקורת עצמית: "מה היית משפר?" 🔍

זו אחת השאלות החשובות בראיון. מראיין רוצה לראות שאתה **מודע למגבלות** של הקוד שלך.
עבור על הנקודות האלו — הן אמיתיות בקוד שלך — והיה מוכן לדבר עליהן בכנות:

21. **אין שכבת ולידציה אמיתית** — איך היית מוסיף ולידציה מסודרת לקלט (למשל אורך מינימלי, סינון תוכן)?
22. **ה-URL של ה-API מקודד קשיח** (`http://localhost:5000`) בכל קומפוננטה.
    למה זו בעיה ל-production ואיך פותרים? (משתנה סביבה, קובץ `services/api.js` מרכזי)
23. **שגיאות מטופלות רק ב-`console.error`** ב-Frontend — המשתמש לא רואה הודעה.
    איך היית משפר את חוויית המשתמש בזמן שגיאה / טעינה?
24. **שמות לא עקביים**: `createAT` במקום `createdAt`, תיקייה `contorollers` עם שגיאת כתיב.
    למה עקביות בשמות חשובה בצוות אמיתי?
25. **`generateConspiracyController` לא שומר במסד** — רק מחזיר טקסט. למה זה בעצם החלטה נכונה כאן?
26. **`DELETE /conspiracies` בלי id מוחק את האחרון שנוצר** ([conspiracyController.js:115](contorollers/conspiracyController.js#L115)).
    למה endpoint כזה מסוכן ומה היית עושה אחרת?
27. אם 1,000 משתמשים היו עולים על האפליקציה מחר — מה היה נשבר ראשון? (אבטחה, pagination, אינדקסים, rate-limiting)

---

## חלק 6 – ידע כללי שעלול לעלות (גם אם לא בפרויקט)

מראיינים נוטים להרחיב מעבר לקוד עצמו:

28. מה ההבדל בין `PUT` ל-`PATCH`? (יש לך `PATCH` ב-router)
29. מה זה REST בכלל? מה הופך API ל-"RESTful"?
30. מה ההבדל בין SQL ל-NoSQL? מתי תבחר כל אחד?
31. מה זה אבטחה בסיסית ב-API? (אימות/Authentication, הרשאות/Authorization, sanitization של קלט)
    — שים לב: בפרויקט שלך **אין** אימות. היה מוכן לומר איך היית מוסיף (JWT, sessions).
32. מה ההבדל בין Frontend ל-Backend ולמה הם רצים בפורטים נפרדים אצלך?
33. מה זה `package.json` ומה ההבדל בין `dependencies` ל-`devDependencies`?
34. למה `.env` נמצא ב-`.gitignore`? מה הסיכון אם הוא היה עולה ל-git?

---

## חלק 7 – Live coding אפשרי (היה מוכן לקודד מולם)

מראיין עשוי לבקש להוסיף משהו קטן בזמן אמת. תרגל את אלו עד שתעשה אותם בלי לחשוב:

1. הוסף endpoint חדש: `GET /conspiracies/:id/comments/count`.
2. הוסף ולידציה: אסור ליצור קונספירציה עם טקסט קצר מ-10 תווים → החזר `400`.
3. הוסף מיון לפי מספר תגובות.
4. הוסף כפתור "מחק" בכרטיסייה ב-React שקורא ל-`DELETE /conspiracies/:id`.
5. הוצא את כתובת ה-API למשתנה סביבה אחד / קובץ קונפיג.

---

## חלק 8 – שאלות התנהגותיות (Behavioral)

לא הכל טכני. הכן סיפור קצר אמיתי לכל אחת (שיטת **STAR**: מצב → משימה → פעולה → תוצאה):

35. ספר על באג קשה שנתקלת בו בפרויקט הזה ואיך פתרת אותו.
36. איך החלטת מאיפה להתחיל כשבנית את הפרויקט מאפס?
37. מה היית עושה אחרת אם היית מתחיל את הפרויקט מחדש?
38. איך אתה ניגש למצב שבו אתה תקוע ולא יודע איך להמשיך? (תיעוד, ניסוי, שאלה)
39. למה אתה רוצה את התפקיד הזה / מה מעניין אותך בתחום?

---

## טיפים אחרונים לראיון 🍀

- **תכין את ה-Pitch של 60 שניות בעל-פה** עד שהוא זורם — זה הרושם הראשוני.
- כשאתה לא יודע תשובה: אל תמציא. אמור "לא נתקלתי בזה, אבל הייתי ניגש לזה ככה..." — מראים חשיבה.
- **תמיד תסביר "למה"**, לא רק "מה". "בחרתי Mongoose כי..." עדיף מ-"השתמשתי ב-Mongoose".
- היה כן לגבי מגבלות הקוד — מודעות עצמית מרשימה יותר מהעמדת פנים שהכל מושלם.
- אם אתה יכול — **תריץ את הפרויקט לפני הראיון** ותדע להראות אותו עובד.
- דבר על הפרויקט בגוף ראשון: "בניתי", "החלטתי", "תיקנתי".

בהצלחה! 💪

</div>
